volumes:
  lldap_data:
    driver: local
  valkey_data:
    driver: local

services:
  lldap:
    build:
      context: ./docker/lldap
    ports:
      # ai-protect's Config::dev_default() proxies to an upstream LDAP
      # server on 127.0.0.1:389, so this container's LDAP port is mapped to
      # host port 389 to act as that upstream for local end-to-end testing.
      - "389:3890"
      # For LDAPS (LDAP Over SSL), enable port if LLDAP_LDAPS_OPTIONS__ENABLED set true, look env below
      #- "6360:6360"
      # For the web front-end
      - "17170:17170"
    volumes:
      - "lldap_data:/data"
      # Alternatively, you can mount a local folder
      # - "./lldap_data:/data"
    environment:
      - UID=1000
      - GID=1000
      - TZ=Etc/UTC
      - LLDAP_JWT_SECRET=${LLDAP_JWT_SECRET}
      - LLDAP_KEY_SEED=${LLDAP_KEY_SEED}
      - LLDAP_LDAP_BASE_DN=dc=example,dc=com
      - LLDAP_LDAP_USER_PASS=${LLDAP_LDAP_USER_PASS} # If the password contains '$', escape it (e.g. Pas$$word sets Pas$word)
      # If using LDAPS, set enabled true and configure cert and key path
      # - LLDAP_LDAPS_OPTIONS__ENABLED=true
      # - LLDAP_LDAPS_OPTIONS__CERT_FILE=/path/to/certfile.crt
      # - LLDAP_LDAPS_OPTIONS__KEY_FILE=/path/to/keyfile.key
      # You can also set a different database:
      # - LLDAP_DATABASE_URL=mysql://mysql-user:password@mysql-server/my-database
      # - LLDAP_DATABASE_URL=postgres://postgres-user:password@postgres-server/my-database
      # If using SMTP, set the following variables
      # - LLDAP_SMTP_OPTIONS__ENABLE_PASSWORD_RESET=true
      # - LLDAP_SMTP_OPTIONS__SERVER=smtp.example.com
      # - LLDAP_SMTP_OPTIONS__PORT=465 # Check your smtp provider's documentation for this setting
      # - LLDAP_SMTP_OPTIONS__SMTP_ENCRYPTION=TLS # How the connection is encrypted, either "NONE" (no encryption, port 25), "TLS" (sometimes called SSL, port 465) or "STARTTLS" (sometimes called TLS, port 587).
      # - LLDAP_SMTP_OPTIONS__USER=no-reply@example.com # The SMTP user, usually your email address
      # - LLDAP_SMTP_OPTIONS__PASSWORD=PasswordGoesHere # The SMTP password
      # - LLDAP_SMTP_OPTIONS__FROM=no-reply <no-reply@example.com> # The header field, optional: how the sender appears in the email. The first is a free-form name, followed by an email between <>.
      # - LLDAP_SMTP_OPTIONS__TO=admin <admin@example.com> # Same for reply-to, optional.
      # - LLDAP_HTTP_URL=https://lldap.example.com # URL used in the email template to compose the reset link, also used to tell LLDAP where it lives in the domain/URL to allow for sub-paths

  valkey:
    # Optional shared-state backend for running multiple ai-protect
    # instances in HA across hosts with no shared disk (the SQLite
    # `state_db` option needs one — see TODO.md "Policy state is
    # process-local and in-memory"). Not started by `docker compose up` on
    # its own; bring it up explicitly with `docker compose --profile ha up`.
    profiles:
      - ha
    build:
      context: ./docker/valkey
    ports:
      - "6379:6379"
    volumes:
      - "valkey_data:/data"
    # `--requirepass` so this example is never an unauthenticated,
    # network-reachable instance by default — anyone who can reach an
    # unauthenticated Valkey/Redis `state_db` can forge or erase the
    # rate-limit history ThresholdPolicy enforces (see ARCHITECTURE.md
    # "Valkey-backed policy state"). Change this password for anything
    # beyond local testing, and point policies/ldap.toml's
    # `[policy.state_db]` username/password at it to match.
    command: valkey-server --save 60 1 --appendonly yes --requirepass change-me-in-production
